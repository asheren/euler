require 'rspec'

describe 'solution' do 

	it "adds the previous two terms to generate the third term"
	expect(answer).to eq(n+n.last)
  end

  it "generates all numbers in a sequence up to four million"
  expect(answer).to eq integer
  end

  it "find the even-valued terms"
  expect(answer).to eq|even|
  end

  it "adds the even-valued terms"
  expect(answer).to eq { sum }
  end

end